import "utils" as u;

on_start {
    print("hi");
    assets::get("avi");
}

on_intent "find_hotel" {
    print(intent.name);
    print(intent.get("default_locations"));
}

on_intent "book_figth" {
    print(intent.name);
    print(intent.get("default_locations"));
}